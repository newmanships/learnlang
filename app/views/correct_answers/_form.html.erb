<%= form_for(@correct_answer) do |f| %>
  <% if @correct_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@correct_answer.errors.count, "error") %> prohibited this correct_answer from being saved:</h2>

      <ul>
      <% @correct_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :correctAnswerText %><br>
    <%= f.text_area :correctAnswerText %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
