<script type="text/javascript">
$(document).on("click", 'elegant-aero-button', function() {
    $.ajax({
                 url: "<%= checkAnswer_user_answers_path %>", // Route to the Script Controller method
                type: "GET",
            dataType: "json",
                data: { userAnswerText }, // This goes to Controller in params hash, i.e. params[:file_name]
            complete: function() {},
             success: function(data, textStatus, xhr) {
                        // Do something with the response here
                        editor.setValue(data.file_content) // Show the file contents in our editor.
                                                           // editor is defined somewhere above 
                                                           // 'file_content' is the key we use to respond
                                                           // in the controller function below
                      },
               error: function() {
                        alert("Ajax error!")
                      }
    });
});
</script>
<div id="notice"><%= notice %></div>


    

    <%= form_for(@user_answer, html: {class: "elegant-aero"})  do |f| %>
    <h1> <%= @question.question_text %></h1>
  <% if @user_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_answer.errors.count, "error") %> prohibited this user_answer from being saved:</h2>

      <ul>
      <% @user_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :question_id, :value => params[:id] %>
  </div>
  <div class="field">
    <br>
    <%= f.text_area :userAnswerText, :placeholder => "Transliterate..." %>
  </div>
  <div class="field">
    <%= hidden_field_tag :attempt_id, 1 %>
  </div>
  <div class="field">
    <%= hidden_field_tag :user_id, current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', class: "elegant-aero button" %>
  </div>
<% end %>