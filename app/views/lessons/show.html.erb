<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @lesson.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @lesson.body %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@lesson.image_url, :width => 600) if @lesson.image.present? %>
</p>

<p>
<h2><%= @lesson.section.title %></h2>
<h1><%= @lesson.course.title %></h2>
<%= link_to 'Edit', edit_lesson_path(@lesson) %> |
<%= link_to 'Back', lessons_path %>
